.venv
node_modules/
# Project specific
SinoPac/
__pycache__/
# shioaji-pro-app — separate project, cloned locally; not committed
shioaji-pro-app/
/results
*.csv
*.json
*.log
*.db

# Stock autocomplete datasets — must be committed so the frontend can serve them
!frontend/public/data/*.json

# Evaluation results
eval_results/
*.egg-info/
.env
